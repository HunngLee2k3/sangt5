extends layout

block content
  .row.justify-content-center
    .col-md-6
      h2.text-center.mt-5 Đặt lại mật khẩu
      if error
        p.text-danger.text-center= error
      form(action=`/auth/reset_password/${token}` method="POST")
        .mb-3
          label.form-label(for="password") Mật khẩu mới
          input.form-control(type="password" id="password" name="password" required)
        .mb-3
          label.form-label(for="confirmPassword") Xác nhận mật khẩu
          input.form-control(type="password" id="confirmPassword" name="confirmPassword" required)
        button.btn.btn-primary.w-100(type="submit") Đặt lại mật khẩu
      p.text-center.mt-3
        a(href="/auth/login") Quay lại đăng nhập